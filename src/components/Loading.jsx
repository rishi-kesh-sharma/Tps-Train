import React from "react";

const Loading = () => {
  return (
    <div className="flex h-[150px] w-full items-center font-semibold text-center justify-center transition-all">
      Loading
    </div>
  );
};

export default Loading;
